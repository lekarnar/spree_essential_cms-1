<%= render :partial => 'spree/admin/shared/page_tabs', :locals => {:current => 'Page Details'} %>

<% if @page.try(:errors).present? %>
  <%= render 'spree/shared/error_messages', :target => @page %>
<% end %>

<%= form_for([:admin, @page]) do |f| %>
  <%= render "form", :form => f %>
  <p class="form-buttons" style="margin-top: 20px;">
    <%= button Spree.t("update") %> <%= Spree.t('or') %> <%= link_to Spree.t('cancel'), collection_url, class: 'btn btn-danger' %>
  </p>
<% end %>
